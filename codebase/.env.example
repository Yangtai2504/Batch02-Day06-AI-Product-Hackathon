# An — Symptom Triage Assistant
# ----------------------------------------------------------------------
# Prototype mặc định chạy bằng "triage engine" mô phỏng (rule-based),
# không cần API key. Để gắn AI thật (Claude), hãy dùng MỘT proxy server
# nhỏ ở backend — KHÔNG đặt API key trực tiếp trong frontend (lộ key).
#
# Xem hướng dẫn trong src/lib/triageEngine.js (hàm callRealModel).
# ----------------------------------------------------------------------

# Endpoint backend proxy của bạn (vd: http://localhost:8787/triage)
VITE_TRIAGE_API_URL=
